spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.xml
  session:
    store-type: jdbc
    jdbc:
      initialize-schema: never
  mail:
    host: ${MAIL_HOST}
    port: ${MAIL_PORT:587}
    username: ${MAIL_USERNAME}
    password: ${MAIL_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  ai:
    ollama:
      # NFR13: Ollama chat HTTP read timeout is capped at 120s in com.examinai.config.AIConfig (no separate starter property).
      base-url: ${OLLAMA_BASE_URL:http://ollama:11434}

github:
  token: ${GITHUB_TOKEN:}

server:
  servlet:
    session:
      timeout: 1h

management:
  endpoints:
    web:
      exposure:
        include: health
